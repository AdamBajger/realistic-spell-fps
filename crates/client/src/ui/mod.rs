/// UI module for spellcasting interface and spell stack
pub struct SpellUI;

impl SpellUI {
    pub fn new() -> Self {
        Self
    }

    pub fn render(&self) {
        // TODO: Render spell UI
    }
}

impl Default for SpellUI {
    fn default() -> Self {
        Self::new()
    }
}
