/// Integration test to verify workspace structure
#[test]
fn test_workspace_compiles() {
    // This test verifies that all crates in the workspace can compile together
    assert!(true, "Workspace structure is valid");
}
